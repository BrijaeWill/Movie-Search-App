const fetchMovie = async () => {
  try {
    // 1. Build your API URL
    //    - Include the API key
    //    - Include the searchTerm from state (s=${searchTerm})

    // 2. Make the fetch call using the URL you built

    // 3. Convert the response to JSON

    // 4. Log the JSON response to see the structure
    //    (this will help you figure out what property to use)

    // 5. Update your state with the correct property from the API response
    //    (use setMovies(data.Search), not data.movies)

  } catch (error) {
    // 6. Handle any errors (network issues, invalid response, etc.)
    //    console.error is fine for now
  }
};
export default fetchMovie
